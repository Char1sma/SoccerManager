<UserControl x:Class="SoccerManager.Client.Views.Modals.FanAdditionView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:SoccerManager.Client.Views.Modals"
             xmlns:modalContext="clr-namespace:SoccerManager.Client.ViewModels.ModalContext"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800"
             d:DataContext="{d:DesignInstance modalContext:FanAdditionViewModel}">
  <StackPanel Orientation="Horizontal"
              Margin="10">

      <TextBlock Text="{DynamicResource FanName}"
                 Margin="10 20"/>
      <TextBox Text="{Binding Name}"
               Height="24"
               Margin="10 20"
               VerticalAlignment="Top"
               Width="140"/>
    </StackPanel>
</UserControl>
